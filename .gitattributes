# --- Normalización de saltos de línea ---
* text=auto eol=lf

# Código Python -> LF siempre (compatibilidad Linux/macOS)
*.py text eol=lf

# Archivos de configuración (texto legible)
*.json text
*.toml text
*.ini text
*.cfg text
*.yml text
*.yaml text

# Documentación / Markdown
*.md text diff=markdown
LICENSE text

# Archivos de datos del juego (assets gráficos y fuentes)
*.ttf binary
*.otf binary
*.woff binary
*.woff2 binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary

# Sonidos y música
*.wav binary
*.mp3 binary
*.ogg binary

# Scripts Windows -> CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Scripts Linux -> LF
*.sh text eol=lf

# Archivos binarios ejecutables (compilados / builds)
*.exe binary
*.dll binary
*.so binary
*.dylib binary

# Archivos de lockfiles (para evitar conflictos suicidas en merges)
poetry.lock merge=union
Pipfile.lock merge=union

# Opcional: SQL o scripts especiales
*.sql diff=sql
*.db diff=sql

# --- Ignorar diffs inútiles en binarios compilados de Python ---
*.pyc binary
__pycache__/* binary
